html
    head
        title= title
        style(type='text/css').
            #dobForm {
                display: block;
                position: absolute;
                top: 50%;
                left: 50%;
                margin-right: -50%;
                transform: translate(-50%, -50%);
            }
            select {
                font-size: 3em;
            }
            a {
                font-size: 3em;
            }
        script(type="text/javascript", src="js/form.js")
    body
        form(id="dobForm")
            select(id="month")
            select(id="day")
            select(id="year")
            a(id="gobutton") Go!