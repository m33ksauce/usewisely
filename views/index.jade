html
    head
        title #{title}
        script(type="text/javascript", src="js/form.js")
    body
        form
            select(id="month")
            select(id="day")
            select(id="year")
            button Go!